# Runtime dependencies for the current Conda environment.
# Keep these versions aligned with the environment used by the startup scripts.
fastapi==0.135.1
uvicorn==0.41.0
httpx==0.28.1
psutil==7.2.2
PyYAML==6.0.3
pydantic==2.12.5
playwright==1.61.0
litellm==1.89.3
# LiteLLM's Responses API encrypts streamed response IDs via PyNaCl.
PyNaCl==1.5.0
